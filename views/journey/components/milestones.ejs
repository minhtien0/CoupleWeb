<!-- Relationship Milestones -->
<div class="bg-white rounded-2xl p-6 shadow-sm border border-rose-100">
    <h2 class="text-lg font-semibold text-gray-800 mb-6">Mốc quan trọng</h2>
    <div class="relative">
        <!-- Timeline line -->
        <div class="absolute left-6 top-0 bottom-0 w-0.5 milestone-line"></div>

        <div class="space-y-6 max-h-80 overflow-y-auto pr-4 timeline">

            <% if (milestones.length > 0) { %>

                <% milestones.forEach(ms => { %>

                    <div class="flex items-start space-x-4">

                        <!-- Dot -->
                        <div class="w-3 h-3 bg-rose-500 rounded-full mt-2 relative z-10"></div>

                        <div class="flex-1 pb-6">
                            <!-- Title + Status -->
                            <div class="flex items-center space-x-2 mb-1">
                                <h3 class="font-medium text-gray-800">
                                    <%= ms.title %>
                                </h3>
                                <span class="text-xs bg-green-100 text-green-700 px-2 py-1 rounded-full">
                                    Hoàn thành
                                </span>
                            </div>

                            <!-- Date + Location -->
                            <p class="text-sm text-gray-600 mb-2">
                                <%= new Date(ms.event_date).toLocaleDateString("vi-VN") %>
                                    <% if (ms.location) { %>
                                        •
                                        <%= ms.location %>
                                            <% } %>
                            </p>

                            <!-- Days Passed -->
                            <div class="text-xs text-gray-500">
                                Cách đây
                                <%= ms.days_passed %> ngày 💕
                            </div>
                        </div>
                    </div>

                    <% }) %>

                        <% } else { %>
                            <p class="text-gray-400 text-sm">Chưa có mốc quan trọng nào, hãy thêm ngay nhé!</p>
                            <% } %>

        </div>


        <!-- Add Custom Milestone -->
        <div class="flex items-start space-x-4">
            <button onclick="addMilestone()" class="w-3 h-3 border-2 border-dashed border-gray-300 rounded-full mt-2 relative z-10 hover:border-rose-400 hover:bg-rose-50 transition-colors"></button>
            <div class="flex-1">
                <button onclick="addMilestone()" class="text-sm text-gray-500 hover:text-rose-600 transition-colors">
                    + Thêm mốc quan trọng
                </button>
            </div>
        </div>
    </div>
</div>